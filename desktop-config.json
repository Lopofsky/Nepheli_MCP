{
    "mcpServers": {
        "filesystem": {
            "command": "npx",
            "args": [
                "-y",
                "@modelcontextprotocol/server-filesystem",
                "$HOMEclaude_playground/"
            ]
        },
        "Nepheli-PMS": {
            "command": "sh",
            "args": [
                "-c",
                "VIRTUAL_ENV='$HOMEclaude_playground/rms_mcp' && cd $VIRTUAL_ENV && source .venv/bin/activate && $HOME.local/bin/uv --directory $PWD pip install . && $HOME.local/bin/uv run src/main.py"
            ]
        }
    }
}